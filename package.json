{
  "name": "wichat-app",
  "version": "1.0.0",
  "description": "**WiChat** is a full-stack, real-time chat application that allows users to communicate instantly with a sleek, customizable interface. It features live user status, image sharing, chat pagination, and a secure authentication system — all wrapped in a beautiful UI with over 30 themes.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install --prefix backend && npm install --prefix frontend && cd frontend && npx vite build",
    "start": "npm run start --prefix backend",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}